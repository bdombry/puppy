// DEPRECATED: Utiliser ShareDogButton à la place
// Ce fichier est conservé pour la compatibilité avec d'anciens imports
import ShareDogButton from './ShareDogButton';

export const CollaboratorManager = (props) => {
  return <ShareDogButton {...props} />;
};

export default CollaboratorManager;
